<html>
  <body>
    <div class="map-header">
      <h1>Land Supply & Transit</h1>
        <p>City Expanded "DX" Zoning to Transit Served Locations</p>
      </div>

      <div id="map-container">
        <div id='map'>
          <nav id="menu"></nav>
        </div>
      </div>
      <div id='image-caption'>
        <p>Interactive Guide: To view detailed area amenities, mouse-in to zoom and press control to rotate</p>
      </div>

<script>
  mapboxgl.accessToken = 'pk.eyJ1IjoibXBvdHRlciIsImEiOiJjajAxZGltM3UwNjF2MzJsczVnN3R2eTNnIn0._Sj0HRLt8VTQGTojMWYFfQ';
  const map = new mapboxgl.Map({
    container: 'map',
    style: 'mapbox://styles/mpotter/cjun4sh9t1dh41fmzib1u329c',
    center: [-87.636127, 41.892699],
    zoom: 12.5,
    maxZoom: 17.0,
  });

//center: [-87.636127, 41.892699],

  map.on('load', function () {
    map.addSource('downtown', {
      type: 'vector',
      url: 'mapbox://mpotter.cj0ww4w5f004j32qdo85d6t1j-41f0i'
    });
    map.addLayer({
      'id': 'downtown',
      'type': 'fill',
      'source': 'downtown',
      'source-layer': 'DowntownZoning',
      'layout': {},
      'paint': {
        'fill-color': '#C22E00',
        'fill-opacity': .25
        }
    });

    map.addSource('northbranch', {
      type: 'vector',
      url: 'mapbox://mpotter.cj1qw66ut00ek2rve2ylsxi7r-41snd'
    });
    map.addLayer({
      'id': 'northbranch',
      'type': 'fill',
      'source': 'northbranch',
      'source-layer': 'NorthBranchZoning',
      'layout': {},
      'paint': {
        'fill-color': '#6DA2A6',
        'fill-opacity': .25
        }
    });

    map.addSource('transit', {
      type: 'vector',
      url: 'mapbox://mpotter.cj1p1jjgm016w2qpi0zsdqylr-3eycz'
    });
    map.addLayer({
      'id': 'transit',
      'type': 'fill',
      'source': 'transit',
      'source-layer': 'Transit_-_Blue_Line',
      'layout': {},
      'paint': {
        'fill-color': '#00a5e0',
        'fill-opacity': .1
        }
    });
  });

var toggleableLayerIds = [ 'downtown', 'northbranch', 'transit' ];

for (var i = 0; i < toggleableLayerIds.length; i++) {
  var id = toggleableLayerIds[i];

  var link = document.createElement('a');
  link.href = '#';
  link.className = 'active';
  link.textContent = id;

  link.onclick = function (e) {
    var clickedLayer = this.textContent;
    e.preventDefault();
    e.stopPropagation();

    var visibility = map.getLayoutProperty(clickedLayer, 'visibility');

    if (visibility === 'visible') {
        map.setLayoutProperty(clickedLayer, 'visibility', 'none');
        this.className = '';
    } else {
        this.className = 'active';
        map.setLayoutProperty(clickedLayer, 'visibility', 'visible');
      }
    };

    var layers = document.getElementById('menu');
    layers.appendChild(link);
    }
</script>


</body>
</html>
